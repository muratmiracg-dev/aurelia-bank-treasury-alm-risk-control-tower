# Optional live TCMB EVDS connector. The committed demo never requires a secret.
EVDS_API_KEY=replace_with_your_own_key
EVDS_BASE_URL=https://evds2.tcmb.gov.tr/service/evds

# API is read-only. Configure a non-empty key only when exposing it outside localhost.
AURELIA_API_KEY=
AURELIA_PROJECT_ROOT=.

